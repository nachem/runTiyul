# RunTiyul v1.4.3

Release status: [published 2026-09-22](https://github.com/nachem/runTiyul/releases/tag/v1.4.3)<br>
App version: `1.4.3+13`

The center-to-view button now preserves your manually chosen zoom. Zooming to
fit the whole route is available through a separate, explicit control.

## Changes

- **MAP-008:** the existing center-focus icon is **Center view (keep zoom)**.
  It recenters around the current route, track, checkpoints, selected area, and
  known location without changing zoom or map orientation.
- Empty-content centering leaves the view unchanged. Center view and GPS
  recenter both preserve manual zoom and cancel pending startup placement.
- A separate **Fit route and content (adjust zoom)** button intentionally
  adjusts zoom to frame everything. Automatic route previews keep their
  existing fit behavior.

## Validation

- Reproduced the previous behavior: center-view changed z17.25 to z15 for a
  single location and to z16 for a route.
- All 28 focused map/control tests passed, covering fractional manual zoom,
  course-up rotation, route and selected-area centering, independent callbacks,
  empty-content startup races, explicit Fit, and automatic route fit.
- Full local Flutter suite: all 312 tests passed. Formatting checked 101 Dart
  files without changes; `flutter analyze --no-pub` passed with no issues.
- Removed an empty test file and an unused local left by earlier committed
  download-editor changes; that existing editor behavior is preserved.
- Hosted [Continuous integration
  35750005333](https://github.com/nachem/runTiyul/actions/runs/35750005333)
  passed formatting, analysis, and the complete test suite for release commit
  `f32b5653efe8dda5605d2e80140d6f93f27204eb`. [CodeQL
  35750004979](https://github.com/nachem/runTiyul/actions/runs/35750004979)
  also passed before tagging.
- [Release workflow
  35750301499](https://github.com/nachem/runTiyul/actions/runs/35750301499)
  passed metadata, signed Android build/identity verification, unsigned iOS
  packaging, checksums, provenance, and publication.
- Independent public downloads match both `SHA256SUMS.txt` and GitHub asset
  digests: APK `d8cf6f549ae9890013c1e7f9c75b7c0eaf51ebd41f5c336c8a81b6e9f2d6a2a0`
  (62,741,316 bytes); IPA
  `1a690c3f18fbeac2bbed32422b858441b7613b53d5cf748b6a570d2034713b30`
  (16,036,646 bytes).
- Android SDK tools verified the downloaded APK's application ID, version
  `1.4.3+13`, and permanent certificate. Both provenance attestations verify
  against the exact tagged commit on GitHub-hosted runners. Stable latest
  APK/IPA URLs return HTTP 200 with matching asset sizes.

## Downloads and Installation

Release assets are available on the
[v1.4.3 GitHub Release](https://github.com/nachem/runTiyul/releases/tag/v1.4.3)
and the [RunTiyul website](https://nachem.github.io/runTiyul/).

| Platform | Asset | Installation |
| --- | --- | --- |
| Android | `RunTiyul.apk` | Install over a matching permanently signed v1.2.2 or later build. Do not uninstall or clear data. |
| iOS | `RunTiyul.ipa` | Unsigned; requires sideloading and is not an App Store release. |

Application ID remains `com.bernoulli.trailrunner.trail_runner`; version code
increases to 13. Android signing retains certificate SHA-256
`d9f8b0d77eddcddd436d945eec37d66513f9a8f1488b5807b5bf50acf32139e5`.
The workflow verifies this identity and supplies checksums and provenance.

## Limitations and Licensing

- Physical-device field behavior and data-preserving installation remain
  unverified; iOS runtime and sideloading also remain unverified.
- Map-provider permissions, dependencies, licensing, and privacy behavior are
  unchanged. This release grants no new download-provider authorization.