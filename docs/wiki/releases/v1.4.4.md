# RunTiyul v1.4.4

Release status: [published 2026-09-23](https://github.com/nachem/runTiyul/releases/tag/v1.4.4)<br>
App version: `1.4.4+14`

Route planning follows mapped bends more reliably and keeps distant points
as explicit direct connections instead of rejecting the edit.

## Changes

- **RTE-003/RTE-011:** sparse waypoints can follow a mapped right-angle or
  winding path without treating the straight chord as recorded geometry.
  Same-level branch endpoints connect at segment interiors; nearby parallel
  ways and grade-separated crossings remain separate.
- Nearby points snap within 40 m. Planning compares nearby candidates to
  prefer a connected path over an isolated fragment. Distant points stay at
  their chosen locations and connect directly when no reasonable mapped path
  is available. The editor reports direct, unmapped segments; navigation
  recovery still requires strictly mapped connections.
- **RTE-009/RTE-012:** local editing and Undo preserve unaffected geometry;
  repeated taps on the same snapped point do not add duplicate controls.
  Whole-route snapping retains off-map stops and matching GPX metadata, and
  reports when direct connections remain.
- **OFF-012:** the tappable current-map picker reaches the developer
  confirmation flow again and displays the resolved `DEV` source after unlock.
  VS Code includes a separate **Running App (authorized raster DEV)** profile
  for separately permitted Esri World Imagery and Topographic testing.
  Ordinary builds retain their provider restrictions.

## Validation

- Full VS Code Flutter test runner: 331 tests passed.
- `flutter analyze --no-pub`: no issues; changed Dart files were formatted.
- `flutter build apk --debug --no-pub
  --dart-define=ALLOW_AUTHORIZED_VIEW_RASTER_DEV_DOWNLOADS=true` succeeded.
  The existing `flutter_tts` Kotlin migration warning remains non-fatal.
- Automated scenarios cover right-angle and S-bends, hairpins, T-junctions in
  both directions, parallel paths, bridges, restricted connectors, off-map
  stops, empty coverage, duplicate taps, editor tap/Undo, SQLite save/reload,
  and GPX metadata preservation.
- Esri tests cover the actual editor confirmation, persisted unlock, provider
  gate, mocked tile endpoint ordering, and provider-specific tile storage.
- Hosted [Continuous integration
  35840448575](https://github.com/nachem/runTiyul/actions/runs/35840448575)
  and [CodeQL
  35840446114](https://github.com/nachem/runTiyul/actions/runs/35840446114)
  passed for tagged commit `23d2f40b9b675fa18c302113dae7f67f3bbc776b`.
- [Release workflow
  35840664878](https://github.com/nachem/runTiyul/actions/runs/35840664878)
  passed metadata, signed Android build/identity, unsigned iOS packaging,
  checksums, provenance, and publication.
- Independent public downloads match `SHA256SUMS.txt` and GitHub asset digests:
  APK `8b53422c5341f80751ae79aa5a0abcbc28252cdeb3c68611a671a9604f6852e8`
  (62,741,312 bytes); IPA
  `803aac20f639ae9cde866c61347b44bc84afb98e571a59f729a7d7ae0c7d2f4a`
  (16,044,660 bytes). Android SDK tools verified package, version, and the
  permanent signing certificate. Both attestations verify against the exact
  tagged workflow and source commit on GitHub-hosted runners. Stable latest
  APK/IPA URLs return HTTP 200 with the matching asset sizes.
- These are synthetic and mocked scenarios, not outdoor or live-provider
  verification. No physical-device installation or iOS runtime validation was
  performed.

## Downloads and Installation

Release assets are available on the
[v1.4.4 GitHub Release](https://github.com/nachem/runTiyul/releases/tag/v1.4.4)
and the [RunTiyul website](https://nachem.github.io/runTiyul/).

| Platform | Asset | Installation |
| --- | --- | --- |
| Android | `RunTiyul.apk` | Install over a matching permanently signed v1.2.2 or later build. Do not uninstall or clear data. |
| iOS | `RunTiyul.ipa` | Unsigned; requires sideloading and is not an App Store release. |

Application ID remains `com.bernoulli.trailrunner.trail_runner`; Android version
code increases to 14. Release signing retains certificate SHA-256
`d9f8b0d77eddcddd436d945eec37d66513f9a8f1488b5807b5bf50acf32139e5`.
Install the permanent-signed release APK over a matching v1.2.2 or later build;
do not uninstall or clear local data. The iOS archive requires sideloading and
is not an App Store release. Public builds retain ordinary provider gates;
the separately authorized Esri DEV capability is not compiled into this release.

## Limitations and Licensing

- Direct connections are user-planned geometry, not verified walkable paths
  or permission to cross private land. Their editor diagnostic is transient,
  not persisted per segment; saved geometry itself remains intact.
- Display-vector data can omit topology, access metadata, and paths. Endpoint
  joins require coordinate-level coincidence; arbitrary crossing lines are
  not inferred as junctions. Long-route device performance remains unverified.
- Esri offline use requires separate permission and the authorized DEV build
  flag plus on-device confirmation. No new production-provider rights are
  granted; attribution, tile limits, pacing, and cooldowns remain enforced.
- Dependencies, schema, signing identity, and location privacy are unchanged.