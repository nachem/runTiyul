# RunTiyul v1.3.1

Release status: [published 2026-08-15](https://github.com/nachem/runTiyul/releases/tag/v1.3.1)<br>
App version: `1.3.1+9`

RunTiyul v1.3.1 fixes navigation prompts leaving music paused after an alert
finishes.

## Highlights

### Music resumes after navigation prompts

- Releases transient audio focus after the final navigation tone or spoken
  prompt so the interrupted music app can continue playback.
- Stops Android low-latency SoundPool cues after their known duration because
  that backend does not provide a playback-completion callback.
- Deactivates the iOS shared audio session with
  `notifyOthersOnDeactivation`, allowing the interrupted audio app to resume.
- Keeps alert ownership generation-aware so a delayed older alert cannot stop
  or release a newer prompt.
- Preserves all existing Tone + voice, Voice, Tones, Haptics only, cadence,
  fallback, and offline behavior.

## Validation

- Dart formatting passed for all 82 source and test files with zero changes.
- `flutter analyze --no-pub`: passed with no issues.
- Full Flutter test suite: all 150 tests passed.
- `flutter build apk --debug --no-pub`: passed. Android SDK inspection reports
  package `com.bernoulli.trailrunner.trail_runner`, `versionName=1.3.1`, and
  `versionCode=9`.
- Navigation feedback coverage verifies final-cue focus release and ensures a
  superseded alert does not release newer alert audio.
- Local wiki links and the CRLF-aware Git diff check passed.
- GitHub Actions [release run
  `31898244766`](https://github.com/nachem/runTiyul/actions/runs/31898244766)
  passed metadata, protected Android signing and identity checks, iOS
  compilation/package, checksums, provenance, and publication. Push CI run
  [`31898235414`](https://github.com/nachem/runTiyul/actions/runs/31898235414)
  and CodeQL run
  [`31898235009`](https://github.com/nachem/runTiyul/actions/runs/31898235009)
  also passed for commit `8cb3695`.
- Independent public downloads match `SHA256SUMS.txt`: APK
  `c2b8663d170fde0ec5ee9408da50d292a98d2eae6b2901305b800d7ac072b479`
  (62,085,536 bytes) and IPA
  `5fd37de95951674f6ece16476148469ed67c78ae70b57756c9f7e1cd89562f48`
  (15,932,247 bytes).
- The public APK reports package `com.bernoulli.trailrunner.trail_runner`,
  `versionName=1.3.1`, `versionCode=9`, and permanent certificate SHA-256
  `d9f8b0d77eddcddd436d945eec37d66513f9a8f1488b5807b5bf50acf32139e5`.
- GitHub build-provenance verification passed for both APK and IPA, and both
  stable `releases/latest/download` URLs returned HTTP 200.

## Downloads and Installation

Stable downloads are available from the
[RunTiyul website](https://nachem.github.io/runTiyul/) and GitHub Releases.

| Platform | File | Installation |
| --- | --- | --- |
| Android | `RunTiyul.apk` | Builds installed from permanently signed `v1.2.2` or later can update in place. Builds from `v1.2.0` or older still require the documented one-time uninstall. |
| iOS | `RunTiyul.ipa` | Unsigned build; sideload with [AltStore](https://altstore.io/) or [Sideloadly](https://sideloadly.io/). It is not distributed through the App Store. |

## Licensing

No licensing terms changed. The app remains MIT licensed. Navigation cues reuse
the bundled Kenney Interface Sounds 1.0 assets under CC0 1.0; source provenance
and hashes remain in `assets/audio/navigation/LICENSE.txt`.

## Known Limitations

- Music resumption after navigation prompts still requires physical Android and
  iOS verification with representative music and podcast apps, Bluetooth or
  open-ear headphones, and background or locked-screen guidance.
- iOS runtime and unsigned IPA sideloading remain unverified.
- Tone audibility outdoors, moving-course quality, and background GPS recording
  still require physical-device verification.
- Production offline-map provider approval remains unresolved.
