# RunTiyul v1.2.1

Release status: prepared 2026-07-21<br>
App version: `1.2.1+6`

RunTiyul v1.2.1 establishes a permanent Android signing identity so releases
after this baseline can install over one another while preserving local data.
It also lets the app report its installed version and recognize later upgrades.

## Highlights

### Android update safety

- Replaced CI-generated debug signing with one protected 4096-bit RSA release
  key stored in GitHub Actions secrets.
- Pinned the public release-certificate SHA-256 fingerprint in the workflow.
- Release builds now fail when signing credentials are missing or incomplete.
- CI rejects a non-increasing Android `versionCode` and verifies the APK's
  package ID, version name, version code, and signing certificate before upload.

### Installed-version awareness

- About shows the installed app version and build number from Android/iOS
  package metadata.
- The app records its first tracked version locally and shows a one-time update
  confirmation after a later build is installed.
- Version recognition is offline and does not send device or location data.

## Important Android Install Note

Android APKs through `v1.2.0` were signed by different ephemeral debug keys.
Android therefore cannot install `v1.2.1` over those builds. Existing users must
uninstall the old app before installing `v1.2.1`; Android normally removes that
installation's routes, activities, settings, and downloaded maps during the
uninstall.

`v1.2.1` is the permanent-signing baseline. Releases after it must retain the
same application ID and certificate and use a higher build number, allowing a
normal in-place update. That two-release physical-device upgrade still needs to
be demonstrated after the next signed version exists.

## Validation

- Dart formatting completed for all changed Dart files.
- `flutter analyze --no-pub`: passed with no issues.
- `flutter test`: all 141 tests passed, including first-install/change
  detection, one-time acknowledgement, update-dialog content, and About version
  display.
- The missing-credential Gradle guard was exercised and failed with the four
  required environment-variable names as intended.
- `flutter build apk --release --no-pub` passed with the protected local key;
  the APK is 61,921,516 bytes.
- Independent Android SDK inspection passed: package
  `com.bernoulli.trailrunner.trail_runner`, `versionName=1.2.1`,
  `versionCode=6`, and certificate SHA-256
  `d9f8b0d77eddcddd436d945eec37d66513f9a8f1488b5807b5bf50acf32139e5`.
- Built APK SHA-256:
  `2fea9cbd7ccd049952868d858ad71cbbc16a782abdd08f24eb8bdf6d742f91ad`.
- Workflow YAML/VS Code diagnostics, build-step secret scoping, website
  JavaScript syntax, release metadata/build monotonicity, local wiki links, and
  the CRLF-aware Git diff check passed. `actionlint` was unavailable locally.

## Downloads

Once published, the stable downloads are available from the
[RunTiyul website](https://nachem.github.io/runTiyul/) and GitHub Releases.

| Platform | File | Installation |
| --- | --- | --- |
| Android | `RunTiyul.apk` | For this signing transition, uninstall any `v1.2.0` or older build, then open the APK. Later permanently signed builds can update in place. |
| iOS | `RunTiyul.ipa` | Unsigned build; sideload with [AltStore](https://altstore.io/) or [Sideloadly](https://sideloadly.io/). It is not distributed through the App Store. |

## Known Limitations

- The permanent certificate and CI checks can be verified now, but an actual
  in-place upgrade requires this baseline and a later APK on a physical device.
- Existing debug-signed installations cannot preserve local data through the
  one-time transition to the permanent key.
- Production offline-map provider approval, iOS runtime verification, and the
  physical-device checks already tracked in the wiki remain unresolved.
